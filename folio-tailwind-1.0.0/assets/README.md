Place your developer photo here

The blog and article pages expect a file at `assets/developer-photo.jpg`.

Steps:
1. Save the image you attached in the repo at `folio-tailwind-1.0.0/assets/developer-photo.jpg`.
2. Start the local server:

```bash
cd folio-tailwind-1.0.0
python3 -m http.server 8000
```

3. Open `http://localhost:8000/blog.html` to preview.

If you want, I can add the file for you if you upload the JPEG directly into the workspace (or provide a URL).